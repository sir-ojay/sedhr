import DefaultLayout from "@/layouts/DefaultLayout";
import { NextPage } from "next";
import React from "react";

const account: NextPage = () => {
	return (
		<DefaultLayout>
			<div className='text-red-500 bg-slate-100 h-full'></div>
		</DefaultLayout>
	);
};

export default account;
